namespace CScourseLab4
{
    public static class Constants
    {
        /// <summary>
        /// This constant used for checking if user want to log out.
        /// </summary>
        public const bool IfUserWantLogOut = false;
        
        /// <summary>
        /// This constant used for checking if user want to continue use program menus. 
        /// </summary>
        public const bool IfUserWantContinue = true;
    }
}