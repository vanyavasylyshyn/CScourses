namespace CScourseLab4
{
    /// <summary>
    /// Enum used for defining statuses of work offer.
    /// </summary>
    public enum WorkOfferStatus
    {
        Opened,
        Closed,
        InProgres
    }
}